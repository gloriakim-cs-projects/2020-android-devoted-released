<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dailybible3">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Credits"></activity>
        <activity android:name=".MyNotes"></activity>
        <activity android:name=".Setting"></activity>

        <activity android:name=".DatabaseHelper"></activity>
        <activity android:name=".ListViewAdapter"></activity>
        <activity android:name=".ListViewDataModel"></activity>
        <activity android:name=".FragmentNotesEditPrivateData"></activity>
        <activity android:name=".NotificationHelper"></activity>
        <activity android:name=".FragmentAlarmPlanInfo"></activity>

<!--        <activity android:name=".InstructionActivity"></activity>-->
        <activity android:name=".InstructionActivity"
            android:theme="@style/Theme.AppCompat.Translucent" />

        <receiver android:name=".AlertReceiver"></receiver>
    </application>

</manifest>